apiVersion: v1
kind: Service
metadata:
  name: vote
  labels:
    app: vote
spec:
  type: LoadBalancer
  ports:
    - name: vote-service
      port: 8080
      targetPort: 80
  selector:
    app: vote

  
